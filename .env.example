# Copy to .env and replace the token with your bot token
DISCORD_TOKEN=your-bot-token-here

# Your bot application's client ID (used to register slash commands)
CLIENT_ID=1440804470819786903

# A test guild ID to register commands during development (guild-scoped registration)
TEST_GUILD_ID=251777999752396811

# Optional: timezone for the schedule (IANA timezone). Defaults to UTC.
TIMEZONE=UTC

# Optional: port for the web health server (some platforms set this automatically)
PORT=3000
